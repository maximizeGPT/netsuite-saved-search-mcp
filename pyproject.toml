[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "netsuite-saved-search-mcp"
version = "0.1.0"
description = "MCP server exposing NetSuite saved-search XML exports as a structured query interface."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Mohammed Wasif" }]
keywords = ["mcp", "netsuite", "saved-search", "claude", "xml", "spreadsheetml"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Accounting",
]
dependencies = [
    "mcp>=1.0",
    "lxml>=5.0",
    "python-dateutil>=2.9",
    "pydantic>=2.6",
]

[project.scripts]
netsuite-saved-search-mcp = "netsuite_saved_search_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/netsuite_saved_search_mcp"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "mypy>=1.10",
    "ruff>=0.5",
    "types-python-dateutil>=2.9",
    "lxml-stubs>=0.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.mypy]
strict = true
python_version = "3.12"
mypy_path = "src"
packages = ["netsuite_saved_search_mcp"]

[[tool.mypy.overrides]]
module = ["dateutil.*"]
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "RUF"]
ignore = ["E501"]
